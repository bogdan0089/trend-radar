[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    # bs4 4.12 передає lxml опцію strip_cdata, яку той оголосив застарілою.
    # Наш код тут ні до чого — глушимо, щоб не ховати справжні попередження.
    ignore:The 'strip_cdata' option:DeprecationWarning
